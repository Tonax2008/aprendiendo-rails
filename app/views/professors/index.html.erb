<%= @professor.each do |professor| %>
    <%= professor.name %>
    <%= link_to 'Editar', edit_professor_path(professor) %>
    <%= link_to 'Eliminar', professor, method: :delete, data:{confirm: 'Seguro deseas eliminarlo?'} %>
    <br>
<% end %> 