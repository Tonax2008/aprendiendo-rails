<h1>Schedule#index</h1>
<%= @schedule.each do |schedule| %>
<%= @schedule.name%>
<%= link_to "Editar", edit_schedule_path(schedule) %> 
<%= link_to 'Eliminar',schedule , method: :delete,data:{confirm: 'Seguro deseas Eliminar'} %>


<br>
<% end %>
